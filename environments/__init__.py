"""Environment package namespace for Verifiers-compatible environments.

Each submodule under this package should expose a `load_environment(**kwargs)`
function that returns a `verifiers` Environment instance.
"""

